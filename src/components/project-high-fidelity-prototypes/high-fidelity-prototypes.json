{
  "collectionName": "components_project_high_fidelity_prototypes_high_fidelity_prototypes",
  "info": {
    "displayName": "high_fidelity_prototypes",
    "icon": "palette"
  },
  "options": {},
  "attributes": {
    "desc": {
      "type": "text",
      "required": true
    },
    "images": {
      "allowedTypes": [
        "images",
        "files",
        "videos",
        "audios"
      ],
      "type": "media",
      "multiple": true,
      "required": true
    }
  }
}
